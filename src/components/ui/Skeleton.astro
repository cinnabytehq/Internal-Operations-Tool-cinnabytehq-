---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
---

<div class={cn('skeleton', className)} style={style} aria-hidden="true"></div>
